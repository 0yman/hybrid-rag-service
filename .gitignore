__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Your documents and the index built from them stay on your machine.
data/index/
data/uploads/

# The benchmark index is rebuilt by `make eval`. The benchmark corpus itself
# (data/benchmark/) IS committed, so evaluation numbers are reproducible from a
# clean clone rather than depending on what Wikipedia says today.
data/benchmark_index/
